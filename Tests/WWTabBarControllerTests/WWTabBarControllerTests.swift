import XCTest
@testable import WWTabBarController

final class WWTabBarControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWTabBarController().text, "Hello, World!")
    }
}
